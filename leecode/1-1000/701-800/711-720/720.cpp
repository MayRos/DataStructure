#include <string>
#include <vector>
#include <iostream>
using namespace std;
class Solution {
public:
    string longestWord(vector<string>& words) {
        int length = words.size();
        for (int i = 0; i < length; i++)
        {
            string str = words[i];
            
        }
        return "";
    }
};
//int  main() {
//    vector<string> in = { "a", "banana", "app", "appl", "ap", "apply", "apple" };
//
//    cout << Solution().longestWord(in);
//}